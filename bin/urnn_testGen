#!/usr/bin/env bash
# urnn - neural network for colorscheme by images

if [[ $# -eq 1 ]] ; then
	cp $1 /tmp/test
    	urnn colors /tmp/test > ~/.config/Xresources.d/test.color
	xrdb ~/.Xresources
else
    echo "need 1 argument"
fi
